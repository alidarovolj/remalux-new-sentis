# 🛡️ Решение проблемы мерцания AR плоскостей

## ❌ Проблема
Плоскости AR каждый кадр **пропадают и появляются**, создавая эффект мерцания. Это происходило из-за:

1. **Слишком агрессивной системы очистки** - плоскости удалялись через 2 секунды если не "посещены"
2. **Нестабильной сегментации** - маска сегментации меняется каждый кадр
3. **Отсутствия гистерезиса** - плоскости удалялись при первом же "пропуске" в маске

## ✅ Решение: Система стабилизации с гистерезисом

### 🔧 Внесенные изменения:

#### 1. **Увеличена задержка удаления**
```csharp
planePersistenceDelay = 8.0f; // Было: 2.0f
```
- Плоскости теперь удаляются только через 8 секунд без обнаружения

#### 2. **Добавлена система гистерезиса**
```csharp
[Header("🛡️ Система гистерезиса плоскостей")]
[SerializeField] private bool enablePlaneHysteresis = true;
[SerializeField, Range(3, 20)] private int maxMissedFrames = 8;
[SerializeField, Range(1, 10)] private int maskProcessingInterval = 3;
```

**Принцип работы:**
- Плоскость должна **пропасть на 8 кадров подряд** перед удалением
- Если плоскость найдена снова - счетчик пропущенных кадров сбрасывается
- Маска обрабатывается только **каждый 3-й кадр** для стабильности

#### 3. **Улучшена логика CleanupOldPlanes**
- **🛡️ Режим гистерезиса:** Подсчет пропущенных кадров
- **🚫 Стандартный режим:** Удаление по времени (может вызывать мерцание)

#### 4. **Добавлена частотная обработка масок**
```csharp
// В OnSegmentationMaskUpdated
if (enablePlaneHysteresis) {
    maskProcessingFrameCounter++;
    if (maskProcessingFrameCounter >= maskProcessingInterval) {
        maskProcessingFrameCounter = 0;
        frameCounter = 0; // Обрабатываем только через интервал
    }
}
```

### 🎯 Быстрая активация

**Метод для активации стабилизации:**
```csharp
[ContextMenu("🛡️ Активировать систему стабилизации")]
public void ApplyStabilizationSettings()
```

**Настройки:**
- `enablePlaneHysteresis = true`
- `maxMissedFrames = 8` кадров
- `maskProcessingInterval = 3` (каждый 3-й кадр)
- `planePersistenceDelay = 8.0f` секунд
- Отключено избыточное логирование

### 📊 Ожидаемый результат

**ДО:**
- ❌ Плоскости мерцают каждый кадр
- ❌ 40 мелких плоскостей для 2 стен
- ❌ Нестабильная визуализация

**ПОСЛЕ:**
- ✅ Стабильные плоскости без мерцания
- ✅ 2-4 крупных плоскости для 2 стен
- ✅ Плавное появление/исчезновение
- ✅ Лучшая производительность

### 🔍 Диагностика

**Логи системы гистерезиса:**
```
🛡️ Плоскость Wall_Plane_123 пропущена 3/8 кадров. Ожидаем...
🛡️ Плоскость Wall_Plane_123 пропущена 8 кадров подряд. Удаляем.
🧹 Удалено 1 плоскостей. Осталось: 4
```

### ⚙️ Настройка параметров

**Для более стабильной работы:**
- Увеличить `maxMissedFrames` до 10-15
- Увеличить `maskProcessingInterval` до 4-5

**Для более быстрой реакции:**
- Уменьшить `maxMissedFrames` до 5-6
- Уменьшить `maskProcessingInterval` до 2

### 🚨 Экстремальная стабилизация

**Когда использовать:**
- ❌ Обычная стабилизация не помогает
- ❌ Плоскости все еще мерцают
- ❌ Создается слишком много мелких плоскостей

**Настройки экстремальной стабилизации:**
- `maxMissedFrames = 20` - плоскость должна пропасть на 20 кадров подряд
- `maskProcessingInterval = 8` - обработка только каждого 8-го кадра
- `planePersistenceDelay = 30.0f` - 30 секунд задержки удаления
- `minPlaneSizeInMeters = 1.5f` - только большие плоскости (1.5м+)
- `minAreaSizeInPixels = 5000` - очень большие области
- `minWallRatio = 0.9f` - 90% пикселей должны быть стенами
- `wallPixelThreshold = 250` - максимальный порог создания

**Активация:**
```
Правый клик на ARManagerInitializer2 → "🚨 Экстремальная стабилизация"
```

### 🚀 Использование

1. **АВТОМАТИЧЕСКАЯ АКТИВАЦИЯ:** Система активируется автоматически при запуске приложения
2. **Активация в редакторе:** Правый клик на компоненте → "🛡️ Активировать систему стабилизации"
3. **Экстремальная стабилизация:** Правый клик → "🚨 Экстремальная стабилизация" (для сложных случаев)
4. **Активация в коде:** `ARManagerInitializer2.Instance.ApplyStabilizationSettings()`
5. **Ручная настройка:** Настроить параметры в секции "🛡️ Система гистерезиса плоскостей"

### 🔧 Отладка и мониторинг

**Новый метод диагностики:**
```csharp
[ContextMenu("📊 Показать состояние гистерезиса")]
public void DebugHysteresisState()
```

**Показывает:**
- ✅ Статус включения гистерезиса
- 📊 Настройки порогов и интервалов 
- 📈 Количество плоскостей и пропусков
- 📋 Детали по каждой плоскости

**Пример вывода:**
```
=== 📊 СОСТОЯНИЕ СИСТЕМЫ ГИСТЕРЕЗИСА ===
🛡️ Система гистерезиса: ВКЛЮЧЕНА
📊 Максимальные пропущенные кадры: 8
⏱️ Интервал обработки маски: каждый 3-й кадр
🔄 Текущий счетчик кадров: 2
📝 Задержка удаления: 8с
📈 Общее количество плоскостей: 4
💾 Плоскостей с счетчиком пропусков: 1
📋 Детали по пропущенным кадрам:
  ├─ Plane_Wall_12: 3/8 пропущено
```

**Использование отладки:**
- В редакторе: Правый клик → "📊 Показать состояние гистерезиса"
- В коде: `ARManagerInitializer2.Instance.DebugHysteresisState()`
- Для мониторинга работы системы в реальном времени

---

## ✅ Итог

Система стабилизации решает проблему мерцания, обеспечивая плавную и стабильную работу AR плоскостей! 